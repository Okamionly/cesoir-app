# CeSoir Heal — 2026-07-06

## Health probe
```
/ -> 200 (0.457603s)
/login -> 200 (0.302003s)
/events -> 307 (0.213984s)
/manifesto -> 307 (0.274072s)
/help -> 307 (0.255047s)
/why-free -> 200 (0.268909s)
/api/health -> 503 (7.267560s)
```

## Health JSON
```json
{"status":"degraded","checks":{"app":"ok","db":"fail"},"version":"9562cbb","env":"production","ts":"2026-07-06T01:07:49.310Z"}
```

## Drift (vs 2026-07-05)

**Version**: `4624708` → `9562cbb` — this is the heal bot's own report commit from yesterday. No real application code was deployed. All app code stable.

**DB failure persisting — day 35**: `db: fail` unchanged for the 35th consecutive day. Supabase project remains paused. No progress since first detected. All authenticated features (login, events, swipe, chat) are broken in production.

**Pattern A — useReducedMotion without mounted-gate**: 32 components flagged, count **stable** vs yesterday. No new regressions, no fixes landed.

**Pattern G — console.* in api/ or lib/**: Only `src/lib/logger.ts` (expected). ✅

**307 redirects on /events /manifesto /help**: Expected auth-gated redirects. ✅

## Fixes applied
Aucun fix nécessaire (Pattern G clean; Pattern A requires manual review — 32 files stable)

## 🔧 Required actions (not auto-fixable)

| Priority | Action |
|----------|--------|
| **P0 CRITICAL** | **Supabase project PAUSED — day 35**. Go to https://supabase.com/dashboard and unpause the project. Login, events, swipe, and chat are all broken for every real user. |
| P0 | After unpausing, verify `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are still valid in Vercel project settings |
| P1 | Smoke-test login + events flow after unpause to confirm no regressions |
| P1 | Add a keep-alive cron or upgrade to Supabase Pro to prevent future auto-pause |
| P2 | Review Pattern A (32 components) — open a ticket for mounted-gate remediation |

## Status
- App: 🔴 **DEGRADED** — db:fail **day 35**, /api/health 503
- Homepage: ✅ Responding (0.46s)
- No new app deploy since heal bot commit
- 307s on /events /manifesto /help: expected auth redirects ✓
- Pattern A: 32 components flagged, count stable ✓
- Pattern G: logger.ts only (expected) ✓
- Next run: 2026-07-07 03:00 Paris
