# CeSoir Heal — 2026-07-05

## Health probe
```
/ -> 200 (1.196188s)
/login -> 200 (0.241222s)
/events -> 307 (0.256286s)
/manifesto -> 307 (0.194671s)
/help -> 307 (0.404283s)
/why-free -> 200 (0.261628s)
/api/health -> 503 (7.284877s)
```

## Health JSON
```json
{"status":"degraded","checks":{"app":"ok","db":"fail"},"version":"4624708","env":"production","ts":"2026-07-05T02:12:40.020Z"}
```

## Drift (vs 2026-07-04)

**🆕 NEW DEPLOY detected**: version changed `2bb8e9a` → `4624708`. Code was pushed to production since yesterday's run. This is the first app change in 31 days.

**DB failure persisting — day 34**: `db: fail` unchanged. Supabase `profiles` table still unreachable. The new deploy did NOT fix the DB issue (expected — this is an infra problem, not a code fix).

**Pattern A — useReducedMotion without mounted-gate**: 32 components flagged, count **stable** vs yesterday. No regressions introduced by the new deploy, no fixes landed.

**Pattern G — console.* in api/ or lib/**: Only `src/lib/logger.ts` flagged (expected). ✅

**307 redirects on /events /manifesto /help**: Expected auth-gated redirects. ✅

## Fixes applied
Aucun fix nécessaire (no console.* violations outside logger.ts; Pattern A requires manual review)

## 🔧 Required actions (not auto-fixable)

| Priority | Action |
|----------|--------|
| **P0 CRITICAL** | **Supabase project PAUSED** — `db: fail` now on day 34. Go to https://supabase.com/dashboard and unpause the project. All authenticated features (login, events, swipe, chat, etc.) are broken for real users. |
| P0 | After unpausing, verify `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` env vars are still valid in Vercel dashboard |
| P1 | New deploy `4624708` is live — smoke-test login + events flow once DB is unpaused to confirm the deploy didn't introduce regressions |
| P1 | Consider upgrading Supabase plan (Pro) or add a keep-alive cron to prevent future auto-pause |
| P2 | Review Pattern A (32 components) — open a ticket for mounted-gate remediation |

## Status
- App: 🔴 **DEGRADED** — db:fail persisting **day 34**, /api/health 503
- Homepage: ✅ Responding (1.20s)
- New deploy: ⚠️ `4624708` live — DB still down, smoke-test needed after unpause
- 307s on /events /manifesto /help: expected auth redirects ✓
- Pattern A: 32 components flagged, count stable ✓
- Pattern G: logger.ts only (expected) ✓
- Next run: 2026-07-06 03:00 Paris
