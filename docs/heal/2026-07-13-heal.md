# CeSoir Heal — 2026-07-13

## Health probe
```
/ -> 200 (0.569212s)
/login -> 200 (0.283273s)
/events -> 307 (0.250489s)
/manifesto -> 307 (0.229368s)
/help -> 307 (0.218735s)
/why-free -> 200 (0.335918s)
/api/health -> 503 (7.299508s)
```

## Health JSON
```json
{"status":"degraded","checks":{"app":"ok","db":"fail"},"version":"36e11dd","env":"production","ts":"2026-07-13T02:45:30.390Z"}
```

## Drift (vs 2026-07-12)

**Version**: `36e11dd` — same heal-bot commit as yesterday (no new application deploy).

**DB failure — day 42**: `db: fail` persists. Database has been down since 2026-06-02 (42 consecutive days). All authenticated routes remain broken in production.

**307 redirect map**:
| Route | Status | Interpretation |
|---|---|---|
| /events | 307 | Auth redirect — expected for protected route |
| /manifesto | 307 | Auth redirect — expected |
| /help | 307 | Auth redirect — expected |

**Pattern A flags** (useReducedMotion without mounted-gate): 32 components flagged — same as previous runs; manual review needed, no safe auto-fix applied.

**Pattern G** (console.* in api/lib): Only hit was `src/lib/logger.ts` — this is the logger's own implementation and is intentional. Auto-fix was attempted but **correctly reverted** to avoid recursive logger calls. No valid console.* leaks found outside the logger.

**Cumulative version history**:
| Period | Version | DB Status |
|---|---|---|
| 2026-04-25 to 2026-06-01 | various | ✅ db:ok |
| **2026-06-02** | — | 🔴 **db:fail STARTS** |
| 2026-06-02 to 2026-07-10 | `94c5844` | 🔴 db:fail, no app deploys |
| 2026-07-11 | `3aa1f2b` | 🔴 db:fail day 40, heal-bot commit only |
| 2026-07-12 | `a298598` | 🔴 db:fail day 41, heal-bot commit only |
| **2026-07-13** | `36e11dd` | 🔴 db:fail **day 42**, heal-bot commit only |

## Fixes applied
Aucun fix necessaire

## Status
- `/api/health` returning **503** with `db:fail` for 42 consecutive days
- App routes (/, /login, /why-free) healthy — 200s
- Protected routes (/events, /manifesto, /help) correctly redirect (307)
- No auto-fixes applied this run

Next run: tomorrow same time
