# CeSoir Heal — 2026-06-02

## Health probe
```
/ -> 200 (0.363288s)
/login -> 200 (0.104993s)
/events -> 307 (0.139973s)
/manifesto -> 307 (0.142885s)
/help -> 307 (0.211307s)
/why-free -> 200 (0.124911s)
/api/health -> 503 (7.306257s)
```

## Health JSON
```json
{"status":"degraded","checks":{"app":"ok","db":"fail"},"version":"1d9ca1b","env":"production","ts":"2026-06-02T01:08:25.992Z"}
```

## Drift vs 2026-06-01

| Route | Yesterday | Today | Delta |
|-------|-----------|-------|-------|
| / | 200 | 200 | ✓ |
| /login | 200 | 200 | ✓ |
| /events | 307 | 307 | ✓ |
| /manifesto | 307 | 307 | ✓ |
| /help | 307 | 307 | ✓ |
| /why-free | 200 | 200 | ✓ |
| /api/health | **200** | **503** | ⚠ REGRESSION |

**Version drift:** `1796668` → `1d9ca1b` — new deploy since yesterday.

**DB status drift:** `ok` → `fail` — Supabase `profiles.select("id").limit(1)` is erroring in production.

## Pattern A — useReducedMotion sans mounted-gate (32 flags, manual review)
Count stable vs yesterday — no new regressions, no fixes (manual ticket recommended).

- src/components/ui/StreakBadge.tsx
- src/components/ui/XPBar.tsx
- src/components/ui/PageHeader.tsx
- src/components/ui/EmptyState.tsx
- src/components/app/ModeCard.tsx
- src/components/profile/AvailabilityBroadcast.tsx
- src/components/profile/VoiceIntroRecorder.tsx
- src/components/landing/SceneController.tsx
- src/components/chat/TypingIndicator.tsx
- src/components/events/EventCard.tsx
- src/components/events/ClosingSoonBar.tsx
- src/components/map/ProfileFlyInCard.tsx
- src/components/map/EventFlyInCard.tsx
- src/components/moments/StreakUnlock.tsx
- src/components/moments/MomentRing.tsx
- src/components/moments/LevelUpCounter.tsx
- src/components/moments/SuperLikeHalo.tsx
- src/components/moments/VerifyConfetti.tsx
- src/components/motion/RackFocus.tsx
- src/components/motion/Magnetic.tsx
- src/components/messages/ConversationRow.tsx
- src/components/messages/TypingIndicator.tsx
- src/components/messages/EmptyConversations.tsx
- src/components/ambient/GradientShift.tsx
- src/components/ambient/FloatEmoji.tsx
- src/components/ambient/BreatheAvatar.tsx
- src/app/(app)/chat/[id]/page.tsx
- src/app/(app)/events/[id]/page.tsx
- src/app/(app)/modes/page.tsx
- src/test/setup.ts
- src/lib/hooks/useInteractive.ts
- src/lib/motion.ts

## Pattern G — console.* in api/ or lib/
`src/lib/logger.ts` is still the only match — excluded (logger implementation, auto-fix would cause recursion). No other files matched.

## Fixes applied
Aucun fix nécessaire (DB failure requires infra intervention, not code fix)

## 🚨 Action required — DB fail

`/api/health` returned 503 for the first time. The health check (`src/app/api/health/route.ts`) performs a live Supabase query:

```ts
client.from("profiles").select("id").limit(1)
```

This returned an error in production (`db: "fail"`). The /api/health probe itself timed out at **7.3s** (vs 0.4s yesterday), suggesting the Supabase instance is either unreachable or overloaded.

**Probable causes (in order of likelihood):**
1. Supabase project paused (free-tier auto-pause after inactivity)
2. `SUPABASE_SERVICE_ROLE_KEY` or `NEXT_PUBLIC_SUPABASE_URL` rotated/expired in Vercel env vars
3. New deploy `1d9ca1b` introduced a breaking migration or RLS change on `profiles`
4. Transient Supabase outage

**Recommended next steps (manual, not auto-fixable):**
- Check Supabase dashboard → project status (paused?)
- Check Vercel → Settings → Environment Variables for key expiry
- Check Supabase logs for errors at `2026-06-02T01:08:xx`
- Roll back deploy `1d9ca1b` if migration is suspected

## Status
- App: ⚠ DEGRADED (db:fail, /api/health 503)
- 307s on /events /manifesto /help are expected auth redirects
- Pattern A: 32 components still flagged, count stable
- Pattern G: logger.ts excluded correctly
- Next run: 2026-06-03 03:00 Paris
