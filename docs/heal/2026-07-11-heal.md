# CeSoir Heal — 2026-07-11

## Health probe
```
/ -> 200 (0.584580s)
/login -> 200 (0.243785s)
/events -> 307 (0.197392s)
/manifesto -> 307 (0.218639s)
/help -> 307 (0.212175s)
/why-free -> 200 (0.272444s)
/api/health -> 503 (7.270508s)
```

## Health JSON
```json
{"status":"degraded","checks":{"app":"ok","db":"fail"},"version":"3aa1f2b","env":"production","ts":"2026-07-11T01:08:20.677Z"}
```

## Drift (vs 2026-07-10)

**Version**: `3aa1f2b` — **NEW DEPLOY** since yesterday's `94c5844`. First application deployment since 2026-06-02 (after 39 days).

**DB failure — day 40**: `db: fail` persists through the new deploy. Database has not recovered. All authenticated features remain broken in production.

**Pattern A — useReducedMotion without mounted-gate**: 32 components flagged (count stable, identical to yesterday — no new regressions, no fixes).

**Pattern G — console.* in api/ or lib/** : Only `logger.ts` itself has `console.*` calls (intentional, guarded by eslint-disable). No other api/ or lib/ files affected. ✅

**Pattern G fix note**: Bot attempted sed substitution on `logger.ts` itself; this would have created infinite recursion (`logger.error` → `log()` → `logger.error` → …). Revert applied immediately. No other files were modified. ✅

## 307 redirect map
| Route | Status | Interpretation |
|---|---|---|
| /events | 307 | Auth redirect — expected for protected route |
| /manifesto | 307 | Auth redirect — expected |
| /help | 307 | Auth redirect — expected |

## Version history
| Date | Version | Event |
|---|---|---|
| 2026-04-25 to 2026-06-01 | various | ✅ db:ok |
| **2026-06-02** | — | 🔴 **db:fail STARTS** |
| 2026-06-02 to 2026-07-10 | `94c5844` | 🔴 db:fail, no app deploys |
| **2026-07-11** | `3aa1f2b` | 🔴 db:fail **day 40**, **new deploy deployed** |

## Fixes applied
- Pattern G: `logger.ts` self-referential substitution detected and reverted (no net change to source tree)
- All other fixes: none required

## Status
- App: 🔴 **DEGRADED** — db:fail **day 40**, /api/health 503
- Homepage / Login / why-free: ✅ Responding fast (< 600ms)
- 307s on /events /manifesto /help: expected auth redirects ✓
- Pattern A: 32 components flagged, count stable ✓
- Pattern G: logger.ts only (expected, skipped) ✓
- **⚠️ NEW**: deploy `3aa1f2b` pushed since last run — db still failing post-deploy

Next run: tomorrow same time (3am Paris)
