# CeSoir Heal — 2026-07-04

## Health probe
```
/ -> 200 (0.781695s)
/login -> 200 (0.241160s)
/events -> 307 (0.234991s)
/manifesto -> 307 (0.222140s)
/help -> 307 (0.246772s)
/why-free -> 200 (0.244134s)
/api/health -> 503 (7.434243s)
```

## Health JSON
```json
{"status":"degraded","checks":{"app":"ok","db":"fail"},"version":"2bb8e9a","env":"production","ts":"2026-07-04T01:08:17.326Z"}
```

## Drift
Last baseline: docs/heal/2026-06-04-heal.md

**⚠️ DEPLOY FROZEN 30 DAYS** — production still on `2bb8e9a` (committed 2026-06-04). No code has been deployed since June 4. All 5 heal commits in the log are chore-only (no app changes).

**DB failure persisting**: `db: fail` was first logged in docs/heal/2026-06-02-heal.md. As of today, the Supabase `profiles` table has been unreachable for **33+ days**. The health route (`src/app/api/health/route.ts:33`) does `client.from("profiles").select("id").limit(1)` — the error is returned from Supabase, not from missing env vars (`unconfigured` would show if keys were absent; `fail` means the query executes but errors).

## Pattern A — useReducedMotion without mounted-gate
32 components flagged (count **stable** vs June 4 baseline — no regressions, no fixes):
- src/components/ui/StreakBadge.tsx, XPBar.tsx, PageHeader.tsx, EmptyState.tsx
- src/components/app/ModeCard.tsx
- src/components/profile/AvailabilityBroadcast.tsx, VoiceIntroRecorder.tsx
- src/components/landing/SceneController.tsx
- src/components/chat/TypingIndicator.tsx
- src/components/events/EventCard.tsx, ClosingSoonBar.tsx
- src/components/map/ProfileFlyInCard.tsx, EventFlyInCard.tsx
- src/components/moments/StreakUnlock.tsx, MomentRing.tsx, LevelUpCounter.tsx, SuperLikeHalo.tsx, VerifyConfetti.tsx
- src/components/motion/RackFocus.tsx, Magnetic.tsx
- src/components/messages/ConversationRow.tsx, TypingIndicator.tsx, EmptyConversations.tsx
- src/components/ambient/GradientShift.tsx, FloatEmoji.tsx, BreatheAvatar.tsx
- src/app/(app)/chat/[id]/page.tsx, events/[id]/page.tsx, modes/page.tsx
- src/test/setup.ts, src/lib/hooks/useInteractive.ts, src/lib/motion.ts

## Pattern G — console.* in api/ or lib/
Only `src/lib/logger.ts` flagged — **expected** (it IS the logger). No other violations. ✅

## 307 redirects on /events /manifesto /help
Expected — auth-gated routes redirect unauthenticated probes. ✅

## Fixes applied
Aucun fix nécessaire (DB failure requires infra intervention; Pattern A requires manual review)

## 🔧 Required actions (not auto-fixable)

| Priority | Action |
|----------|--------|
| **P0 CRITICAL** | **Supabase project almost certainly PAUSED** — free tier pauses after ~7 days inactivity. DB has been `fail` for 33+ days. Go to https://supabase.com/dashboard and unpause the project. All authenticated features (login, events, swipe, etc.) are broken for users. |
| P0 | After unpausing, verify `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` env vars are still valid in Vercel dashboard |
| P1 | Consider upgrading Supabase plan (Pro) to prevent auto-pause, or set up a keep-alive cron if staying on free tier |
| P2 | Review Pattern A (32 components) — open a ticket for mounted-gate remediation |

## Status
- App: 🔴 **DEGRADED** — db:fail persisting **day 33**, /api/health 503
- Homepage: ✅ Responding (0.78s)
- 307s on /events /manifesto /help: expected auth redirects ✓
- Deploy: ⚠️ Frozen at 2bb8e9a (June 4, 30 days stale)
- Pattern A: 32 components flagged, count stable ✓
- Pattern G: logger.ts only (expected) ✓
- Next run: 2026-07-05 03:00 Paris
