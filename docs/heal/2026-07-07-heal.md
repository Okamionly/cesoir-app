# CeSoir Heal — 2026-07-07

## Health probe
```
/ -> 200 (0.564498s)
/login -> 200 (0.235205s)
/events -> 307 (0.253165s)
/manifesto -> 307 (0.223570s)
/help -> 307 (0.205322s)
/why-free -> 200 (0.358483s)
/api/health -> 503 (7.329481s)
```

## Health JSON
```json
{"status":"degraded","checks":{"app":"ok","db":"fail"},"version":"c7a1fba","env":"production","ts":"2026-07-07T01:07:59.748Z"}
```

## Drift (vs 2026-07-06)

**Version**: `c7a1fba` unchanged — that is yesterday's heal bot commit. No real application code has been deployed.

**DB failure persisting — day 36**: `db: fail` unchanged for the **36th consecutive day**. Supabase project remains paused. Every authenticated feature (login, events, swipe, chat) is broken in production for all real users.

**Pattern A — useReducedMotion without mounted-gate**: 32 components flagged, count **stable** vs yesterday. No new regressions, no fixes landed.

**Pattern G — console.* in api/ or lib/**: `logger.ts` only (expected; internal console usage is intentional). ✅ No other files affected.

**307 redirects on /events /manifesto /help**: Expected auth-gated redirects. ✅

## Fixes applied
Aucun fix nécessaire (Pattern G clean — only logger.ts which is intentional; Pattern A requires manual review — 32 files stable)

> Note: Pattern G sed script incorrectly targeted `logger.ts` (the logger itself uses console.* internally). Reverted immediately. logger.ts should be excluded from this pattern. Consider adding it to an exclusion list.

## 🔧 Required actions (not auto-fixable)

| Priority | Action |
|----------|--------|
| **P0 CRITICAL** | **Supabase project PAUSED — day 36**. Go to https://supabase.com/dashboard and unpause the project. Login, events, swipe, and chat are all broken for every real user. |
| P0 | After unpausing, verify `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are still valid in Vercel project settings |
| P1 | Smoke-test login + events flow after unpause to confirm no regressions |
| P1 | Add a keep-alive cron or upgrade to Supabase Pro to prevent future auto-pause |
| P2 | Review Pattern A (32 components) — open a ticket for mounted-gate remediation |
| P3 | Exclude `src/lib/logger.ts` from Pattern G sed sweep (uses console.* intentionally) |

## Status
- App: 🔴 **DEGRADED** — db:fail **day 36**, /api/health 503
- Homepage: ✅ Responding (0.56s)
- No new app deploy since heal bot commit
- 307s on /events /manifesto /help: expected auth redirects ✓
- Pattern A: 32 components flagged, count stable ✓
- Pattern G: logger.ts only (expected, skipped) ✓
- Next run: 2026-07-08 03:00 Paris
