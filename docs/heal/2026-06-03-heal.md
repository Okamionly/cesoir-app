# CeSoir Heal — 2026-06-03

## Health probe
```
/ -> 200 (3.754148s)
/login -> 200 (0.131957s)
/events -> 307 (0.118737s)
/manifesto -> 307 (0.125355s)
/help -> 307 (0.104104s)
/why-free -> 200 (0.129957s)
/api/health -> 503 (7.569683s)
```

## Health JSON
```json
{"status":"degraded","checks":{"app":"ok","db":"fail"},"version":"10798a4","env":"production","ts":"2026-06-03T01:08:31.546Z"}
```

## Drift vs 2026-06-02

| Route | Yesterday | Today | Delta |
|-------|-----------|-------|-------|
| / | 200 (0.36s) | 200 (3.75s) | ⚠ SLOW ×10 |
| /login | 200 (0.10s) | 200 (0.13s) | ✓ |
| /events | 307 (0.14s) | 307 (0.12s) | ✓ |
| /manifesto | 307 (0.14s) | 307 (0.13s) | ✓ |
| /help | 307 (0.21s) | 307 (0.10s) | ✓ |
| /why-free | 200 (0.12s) | 200 (0.13s) | ✓ |
| /api/health | 503 (7.3s) | 503 (7.6s) | PERSISTENT (day 2) |

**Version drift:** `1d9ca1b` → `10798a4` — new deploy since yesterday.

**DB status:** `db:fail` — **persisting for a 2nd consecutive day, unresolved.**

## 🚨 Regressions

### 1. DB failure — DAY 2 (critical, escalating)

`/api/health` 503 with `db:fail` for the **second consecutive day**. The Supabase health check (`profiles.select("id").limit(1)`) is still erroring in production. A new deploy `10798a4` landed since yesterday — if it included a migration, it may have compounded the issue.

**Still-probable causes (unchanged from 2026-06-02):**
1. Supabase project paused (free-tier auto-pause — most likely if still unresolved)
2. Env var `SUPABASE_SERVICE_ROLE_KEY` / `NEXT_PUBLIC_SUPABASE_URL` expired or rotated
3. Breaking migration on `profiles` table in `10798a4`
4. Persistent Supabase outage (unlikely after 48 h)

**Escalation required today.** This is not a transient blip.

### 2. Homepage slowdown — NEW

`/` response time: **3.75s** today vs **0.36s** yesterday (×10 regression). The homepage likely awaits a DB or Supabase call during SSR that now times out before falling back. Coincides with the DB failure — SSR may be blocking on a failed Supabase fetch with a long timeout.

**Probable cause:** SSR data-fetch on `/` (e.g. `createServerComponentClient`) is waiting for Supabase, timing out after several seconds, then rendering the fallback. Fix: add `{ next: { revalidate: 60 } }` or a short request timeout to the Supabase client in the home page server component, or guard the fetch with a try/catch + short deadline.

## Pattern A — useReducedMotion sans mounted-gate
**32 files flagged — count STABLE vs yesterday (no new regressions, no fixes).**

Manual ticket recommended to address these systematically. List unchanged from 2026-06-02.

## Pattern G — console.* in api/ or lib/
`src/lib/logger.ts` is the only match — excluded (logger implementation, auto-fix would cause recursion). No other files matched. ✓

## Fixes applied
Aucun fix nécessaire (DB failure requires infra intervention; homepage slowdown is a consequence of DB failure)

## 🔧 Recommended actions (manual, not auto-fixable)

| Priority | Action |
|----------|--------|
| P0 | Check Supabase dashboard — is the project paused? Unpause it. |
| P0 | Check Vercel env vars `SUPABASE_SERVICE_ROLE_KEY` and `NEXT_PUBLIC_SUPABASE_URL` |
| P1 | Check if deploy `10798a4` included a DB migration; inspect Supabase migration logs |
| P1 | Add a short timeout/fallback to SSR Supabase fetch on `/` to fix the 3.75s slowdown |
| P2 | Review Pattern A (32 components) — open a ticket for mounted-gate remediation |

## Status
- App: 🔴 DEGRADED (db:fail persisting day 2, /api/health 503, / slow ×10)
- 307s on /events /manifesto /help: expected auth redirects ✓
- Pattern A: 32 components flagged, count stable ✓
- Pattern G: logger.ts excluded correctly ✓
- Next run: 2026-06-04 03:00 Paris
