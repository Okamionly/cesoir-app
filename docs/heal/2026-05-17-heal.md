# CeSoir Heal — 2026-05-17

## Health probe
```
/ -> 200 (0.245954s)
/login -> 200 (0.143366s)
/events -> 307 (0.109043s)
/manifesto -> 307 (0.707919s)
/help -> 307 (0.994254s)
/why-free -> 200 (0.130670s)
/api/health -> 200 (0.511159s)
```

## Health JSON
```json
{"status":"ok","checks":{"app":"ok","db":"ok"},"version":"a5599e6","env":"production","ts":"2026-05-17T01:01:54.612Z"}
```

## Drift
Last baseline: docs/heal/2026-05-16-heal.md

| Item | Yesterday | Today | Delta |
|------|-----------|-------|-------|
| version | ba34bc2 | a5599e6 | **NEW DEPLOY** |
| app | ok | ok | — |
| db | ok | ok | — |
| / status | 200 | 200 | — |
| /login status | 200 | 200 | — |
| /events status | 307 | 307 | — |
| /manifesto status | 307 | 307 | — |
| /help status | 307 | 307 | — |
| /why-free status | 200 | 200 | — |
| /api/health status | 200 | 200 | — |
| / latency | 0.669s | 0.246s | -0.423s (improved) |
| /why-free latency | 1.001s | 0.131s | -0.870s (recovered) |
| /help latency | 0.120s | 0.994s | +0.874s (watch, near 1s) |
| /manifesto latency | 0.127s | 0.708s | +0.581s (watch) |
| Pattern A flags | 32 files | 32 files | — (no regression, no progress) |

> **Note:** `version` changed `ba34bc2` → `a5599e6` — a new deploy occurred between
> runs. All health checks remain green. `/why-free` recovered from its 1.000s spike
> yesterday. `/help` is now approaching 1s (0.994s) — first time it spiked; watch tomorrow.
> `/manifesto` latency also elevated at 0.708s; both are 307 redirects so redirect target
> latency is included. Pattern A (useReducedMotion without mounted-gate) remains at 32
> files — unchanged since at least 2026-05-15; this is a standing manual review item.

## Fixes applied
Aucun fix nécessaire.

> **Pattern G audit note:** `src/lib/logger.ts` was the only file matching
> `console\.(log|warn|error)` in `src/lib/`. The sed replacement was intentionally
> reverted — `logger.ts` legitimately calls `console.*` as its own transport layer;
> replacing those calls would create infinite recursion (`logger.error → log → logger.error`).
> Pattern G exclusion rule: skip `src/lib/logger.ts` in future runs.

## Pattern A standing flags (32 files — manual review required)
These components use `useReducedMotion` without a `mounted` gate, risking hydration
mismatch (server renders with motion, client re-renders without). Not auto-fixable
without understanding each component's render shape.

Top priority candidates (animation-heavy, user-visible):
- `src/components/moments/VerifyConfetti.tsx`
- `src/components/moments/LevelUpCounter.tsx`
- `src/components/moments/StreakUnlock.tsx`
- `src/components/ambient/BreatheAvatar.tsx`
- `src/components/landing/SceneController.tsx`

## Status
- All routes: **green** (200/307 as expected)
- DB: **ok**
- Version: **a5599e6** (new deploy since yesterday)
- `/help` latency: **watch** (0.994s, near threshold)
- Pattern G: **no action taken** (false positive in logger.ts — add exclusion)
- Pattern A: **32 files flagged** — manual sprint item

Next run: 2026-05-18 03:00 Paris
