# CeSoir Heal — 2026-04-24

## Health probe
```
/ -> 200 (0.187603s)
/login -> 200 (0.220649s)
/events -> 307 (0.125843s)
/manifesto -> 307 (0.145706s)
/help -> 307 (0.134717s)
/why-free -> 200 (0.142871s)
/api/health -> 200 (0.804803s)
```

**Notes:**
- `/events`, `/manifesto`, `/help` return 307 redirects (expected — auth-gated routes)
- All routes responsive, no 5xx
- `/api/health` is the slowest at 804ms (DB round-trip included, within normal range)

## Health JSON
```json
{"status":"ok","checks":{"app":"ok","db":"ok"},"version":"b902eaa","env":"production","ts":"2026-04-24T22:59:09.320Z"}
```

## Drift
Last baseline: none (first run)

Last commit touched:
- `src/components/app/KeyboardShortcuts.tsx` (+18/-9 lines)
- `src/components/feed/EventsWidget.tsx` (+17/-2 lines)

## Pattern A — useReducedMotion without mounted-gate (manual review flags)

26 files flagged. These use `useReducedMotion` but lack a `mounted` guard, which can cause SSR hydration mismatches. **No auto-fix applied** — requires case-by-case review.

Notable files:
- `src/app/(app)/layout.tsx`
- `src/lib/motion.ts`
- `src/lib/hooks/useInteractive.ts`
- `src/components/motion/RackFocus.tsx`
- `src/components/motion/Magnetic.tsx`
- `src/components/ambient/` (FloatEmoji, BreatheAvatar, GradientShift)
- `src/components/moments/` (VerifyConfetti, SuperLikeHalo, StreakUnlock, LevelUpCounter)

## Pattern G — console.* in api/lib

Only hit: `src/lib/logger.ts` — intentional (logger implementation wraps `console.*`). **No fix applied.**

## Fixes applied
Aucun fix nécessaire

## Status
- App: **healthy** ✓
- DB: **ok** ✓
- Version: `b902eaa`
- Action items: 26 files flagged for mounted-gate review (Pattern A)
- Next run: 2026-04-25 03:00 Paris
