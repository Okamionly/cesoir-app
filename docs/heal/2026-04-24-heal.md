# CeSoir Daily Heal Report — 2026-04-24

Run at: 2026-04-24 ~22:50 UTC (automated patrol, day 5/Fri)

---

## Phase 1 — Health Probe

| Route | Status | Latency | Notes |
|---|---|---|---|
| `/` | 200 | ~1981ms | OK — under 3s threshold |
| `/login` | 200 | ~640ms | OK |
| `/events` | 307 | ~733ms | Expected auth redirect → `/login?redirectTo=%2Fevents` |
| `/manifesto` | 307 | ~353ms | Expected auth redirect → `/login?redirectTo=%2Fmanifesto` |
| `/help` | 307 | ~321ms | Expected auth redirect → `/login?redirectTo=%2Fhelp` |
| `/why-free` | 200 | ~600ms | OK |
| `/api/health` | 200 | ~2285ms | OK |

**API Health JSON:** `{"status":"ok","checks":{"app":"ok","db":"ok"},"version":"b902eaa","env":"production"}`

**HTML `<title>` present:** Yes — `CeSoir — Trouve quelqu'un pour ce soir`

No 5xx errors. No critical alerts.

---

## Phase 2 — Drift vs Previous Baseline

**No previous baseline exists.** This is the first heal run. Today's report becomes the baseline.

Drift: **N/A (first run)**

---

## Phase 3 — Patterns Scanned

### Pattern A — Hydration mismatch from useReducedMotion

**Files without mounted gate (fixed):**

- `src/components/ambient/BreatheAvatar.tsx` — `if (reduced)` returned `<div>` vs `<m.div>` with no mounted gate
- `src/components/ambient/FloatEmoji.tsx` — `if (reduced)` returned `<span>` vs `<m.span>` with no mounted gate
- `src/components/ambient/GradientShift.tsx` — `if (reduced)` returned `<span|div>` vs `<m.span|m.div>` with no mounted gate
- `src/components/moments/StreakUnlock.tsx` — `if (reduced)` returned plain spans vs `<m.span>` with no mounted gate

**Not fixed (returns `null` when reduced, low SSR risk since `active` starts false):**

- `src/components/moments/SuperLikeHalo.tsx` — `if (!active || reduced) return null` — requires human review
- `src/components/moments/VerifyConfetti.tsx` — same pattern — requires human review

**Already correct:**

- `src/components/motion/PageTransition.tsx` — reference implementation, mounted gate present
- `src/components/ui/ReducedMotion.tsx` — mounted gate present
- All others using `useReducedMotion` for animation props only (no DOM shape branching)

### Pattern B — Missing Suspense around useSearchParams

All 5 files checked — all already have `<Suspense fallback={null}>` wrappers. No fixes needed.

- `src/app/(app)/feed/page.tsx` ✓
- `src/app/(app)/browse/page.tsx` ✓
- `src/app/(app)/plans/page.tsx` ✓
- `src/app/(app)/plans/create/page.tsx` ✓
- `src/app/(auth)/register/page.tsx` ✓

### Pattern C — next/image hostname missing from remotePatterns

All `Image` hardcoded `https://` sources match `remotePatterns`. No drift found.

Current remotePatterns: `images.unsplash.com`, `ui-avatars.com`, `api.dicebear.com`, `ycyxmvzilzkusecpgvbi.supabase.co`, `randomuser.me`, `i.pravatar.cc`

### Pattern D — CSP img-src drift vs remotePatterns

All remotePatterns hostnames are covered by `img-src` in CSP (supabase covered by `*.supabase.co` wildcard). No drift.

### Pattern E — Non-canonical PageTransition imports

No non-canonical imports found. All consumers route through `@/components/motion/PageTransition` or the `@/components/ui/PageTransition` re-export shim.

### Pattern F — Hex leakage in tsx files (DO NOT AUTO-FIX — design team work)

**Top 3 offenders (first baseline, all flagged for design token migration):**

1. `src/components/landing/PhoneVideo.tsx` — 28 hex occurrences
2. `src/components/landing/MoonHero.tsx` — 22 hex occurrences
3. `src/components/landing/SceneController.tsx` — 11 hex occurrences

### Pattern G — console.log/warn/error in api/ and lib/

No console calls found outside `src/lib/logger.ts` (the logger itself). No fixes needed.

---

## Fixes Applied

| Pattern | File | Change |
|---|---|---|
| A | `src/components/ambient/BreatheAvatar.tsx` | Added `useState(false)` + `useEffect` mounted gate; `if (!mounted \|\| reduced)` |
| A | `src/components/ambient/FloatEmoji.tsx` | Same mounted-gate pattern |
| A | `src/components/ambient/GradientShift.tsx` | Same mounted-gate pattern |
| A | `src/components/moments/StreakUnlock.tsx` | Same mounted-gate pattern |

**Total fixes: 4**

---

## Fixes Suggested — Human Review Required

- **Pattern A — SuperLikeHalo + VerifyConfetti**: Both use `if (!active || reduced) return null`. Could cause hydration mismatch if parent ever SSR-mounts with `active=true`. Low risk in current usage (parent controls `active` via client-side state), but should add `if (!mounted) return null` after the `if (!active)` guard. Requires design review on flash behavior.

- **Pattern F — Hex token migration**: 3+ landing components (PhoneVideo, MoonHero, SceneController) contain large numbers of inline hex values. Should migrate to design tokens. Design team work.

---

## Next Run

2026-04-25 03:00 Paris time (01:00 UTC).
