# CeSoir Heal — 2026-07-10

## Health probe
```
/ -> 200 (0.827798s)
/login -> 200 (0.254618s)
/events -> 307 (0.326496s)
/manifesto -> 307 (0.243214s)
/help -> 307 (0.210950s)
/why-free -> 200 (0.264868s)
/api/health -> 503 (7.307113s)
```

## Health JSON
```json
{"status":"degraded","checks":{"app":"ok","db":"fail"},"version":"94c5844","env":"production","ts":"2026-07-10T01:08:08.387Z"}
```

## Drift (vs 2026-07-07)

**Version**: `94c5844` — unchanged from 2026-07-07 (last commit was this bot's heal commit). No real application code has been deployed since 2026-06-02.

**DB failure persisting — day 39**: `db: fail` first appeared on 2026-06-02 and has not recovered. Every authenticated feature (login, events, swipe, chat) remains broken in production for all real users. The Supabase free-tier project is auto-paused after 1 week of inactivity.

**Pattern A — useReducedMotion without mounted-gate**: 32 components flagged (stable count, no new regressions, no fixes landed). Files are unchanged from previous run.

**Pattern G — console.* in api/ or lib/**: `logger.ts` only (intentional — it is the logger itself). No other api/ or lib/ files affected. ✅

**307 redirects on /events /manifesto /help**: Expected auth-gated redirects. ✅

## Fixes applied
Aucun fix nécessaire

- Pattern G: Only `logger.ts` found in lib/ — excluded (intentional console.* usage by the logger itself)
- Pattern A: Requires manual review — 32 components stable, no auto-fix applied

## 🔧 Required actions (not auto-fixable)

| Priority | Action |
|----------|--------|
| **P0 CRITICAL** | **Supabase project PAUSED — day 39**. Go to https://supabase.com/dashboard and unpause the project. Login, events, swipe, and chat are broken for ALL real users. |
| P0 | After unpausing, verify `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are still valid in Vercel project settings |
| P1 | Smoke-test login + events flow after unpause to confirm no regressions |
| P1 | Add a keep-alive cron (e.g. ping `/api/health` every 3 days) or upgrade to Supabase Pro to prevent future auto-pause |
| P2 | Review Pattern A (32 components) — consider a ticket for mounted-gate remediation |
| P3 | Exclude `src/lib/logger.ts` from Pattern G exclusion list to prevent false positives |

## DB failure timeline
| Date | Status |
|------|--------|
| 2026-04-25 to 2026-06-01 | ✅ db:ok |
| **2026-06-02** | 🔴 **db:fail STARTS** |
| 2026-07-10 (today) | 🔴 **db:fail day 39** |

## Status
- App: 🔴 **DEGRADED** — db:fail **day 39**, /api/health 503
- Homepage: ✅ Responding (0.83s)
- No new app deploy since 2026-06-02
- 307s on /events /manifesto /help: expected auth redirects ✓
- Pattern A: 32 components flagged, count stable ✓
- Pattern G: logger.ts only (expected, skipped) ✓

Next run: 2026-07-11 03:00 Paris
